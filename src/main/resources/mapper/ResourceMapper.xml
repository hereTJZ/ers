<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.ers.dao.ResourceMapper">
    <select id="getFiveResources" resultType="Resource">
        select id, title, subtitle, link, create_time
        from resource
        order by create_time desc -- asc相反为升序
        limit 5 -- 查询数量为5条
    </select>
</mapper>